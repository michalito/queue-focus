[package]
name = "qf-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Task model and storage for queue-focus"

[dependencies]
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
